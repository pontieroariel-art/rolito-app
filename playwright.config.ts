import { defineConfig, devices } from '@playwright/test'

// Smoke test mínimo: solo verifica que la app levanta y renderiza el
// selector de login (Landing). No requiere emuladores corriendo — la página
// no autenticada no llega a pegarle a Firestore/Auth.
export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  retries: process.env.CI ? 1 : 0,
  reporter: 'list',
  use: {
    baseURL: 'http://localhost:5173',
    trace: 'on-first-retry',
  },
  projects: [
    { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
  ],
  webServer: {
    command: 'npm run dev',
    url: 'http://localhost:5173',
    reuseExistingServer: !process.env.CI,
    timeout: 30_000,
  },
})
